---
import type { ContainerProps } from '@/components/types'

interface Props extends ContainerProps {}

const { as: Tag = 'div', class: extraClass, unlimited = false, ...restProps } = Astro.props
---

<Tag class:list={[{ 'mx-auto max-w-6xl': !unlimited }, extraClass]} {...restProps}>
  <slot />
</Tag>
