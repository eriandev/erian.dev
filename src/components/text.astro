---
import type { TextProps as Props } from '@/components/types'

const { as: Tag = 'p', class: extraClass, featured = false } = Astro.props
---

<Tag class:list={[{ 'text featured': featured }, extraClass]}>
  <slot />
</Tag>
