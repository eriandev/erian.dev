---
import type { ButtonProps as Props } from '@/components/types.ts'

const { class: extraClass, variant, ...restProps } = Astro.props
---

<button class:list={['btn', variant, extraClass]} {...restProps}>
  <slot />
</button>
